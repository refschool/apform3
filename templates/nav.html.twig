<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Logo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
            </li>

            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item active">
                <a class="nav-link" href="{{path('userList')}}">Administration utilisateurs</a>
            </li>
            {% endif %}

            {% if is_granted('ROLE_STAFF') %}
            <li class="nav-item">
                <a class="nav-link" href="{{path('admin_category')}}">Liste catégories</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('admin_ajoutProduit')}}">Ajout Produit</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('admin_ajoutCategory')}}">Ajout Categorie</a>
            </li>
            {% endif %}


        </ul>
        <div class="form-inline my-2 my-lg-0">
            {% if app.user %}
            <a href="{{ path('app_logout')}}">
                <p class="btn btn-outline-success my-2 my-sm-0">Se
                    déconnecter de {{ app.user.email}}</p>
            </a>
            {% endif %}

            {% if not app.user %}
            <a href="{{ path('app_login')}}">
                <p class="btn btn-outline-success my-2 my-sm-0">Connexion</p>
            </a>
            {% endif %}
        </div>

    </div>
</nav>