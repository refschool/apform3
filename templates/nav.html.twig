<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Logo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('category')}}">Liste catégories</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('ajoutProduit')}}">Ajout Produit</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('ajoutCategory')}}">Ajout Categorie</a>
            </li>
        </ul>
        <div class="form-inline my-2 my-lg-0">
            {% if app.user %}

            <button class="btn btn-outline-success my-2 my-sm-0" type="submit"><a href="{{ path('app_logout')}}">Se
                    déconnecter de {{ app.user.email}}</a></button>
            {% endif %}

            {% if not app.user %}
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit"><a
                    href="{{ path('app_login')}}">Connexion</a></button>
            {% endif %}
        </div>

    </div>
</nav>